import 'package:flutter/material.dart';

class TabletConfirmInfo extends StatelessWidget {
  const TabletConfirmInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}