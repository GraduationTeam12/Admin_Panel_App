import 'package:flutter/material.dart';

class MobileAddOwner extends StatelessWidget {
  const MobileAddOwner({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}