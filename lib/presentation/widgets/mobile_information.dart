import 'package:flutter/material.dart';

class MobileInformation extends StatelessWidget {
  const MobileInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}