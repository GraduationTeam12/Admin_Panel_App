import 'package:flutter/material.dart';

class TabletOtp extends StatelessWidget {
  const TabletOtp({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}