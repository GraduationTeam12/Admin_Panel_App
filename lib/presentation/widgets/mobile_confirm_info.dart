import 'package:flutter/material.dart';

class MobileConfirmInfo extends StatelessWidget {
  const MobileConfirmInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}