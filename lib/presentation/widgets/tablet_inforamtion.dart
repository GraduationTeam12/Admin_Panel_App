import 'package:flutter/material.dart';

class TabletInforamtion extends StatelessWidget {
  const TabletInforamtion({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}